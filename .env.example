# ── Groq (LLM inference) ──────────────────────────────────────────
# Free tier key: https://console.groq.com → API Keys
# Model used: llama-3.1-8b-instant
GROQ_API_KEY=gsk_your_key_here

# ── MinIO (local S3) ──────────────────────────────────────────────
# These are fine for local dev; change before any shared deployment
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
